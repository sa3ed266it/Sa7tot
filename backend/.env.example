APP_ENV=development
DATABASE_URL=postgresql+asyncpg://localhost/sa7tot

# Tests require a separate disposable PostgreSQL database and APP_ENV=test.
# TEST_DATABASE_URL=postgresql+asyncpg://localhost/sa7tot_test

# Required later when Supabase Auth / Apple Sign In is enabled:
# SUPABASE_URL=https://your-project.supabase.co

# APNs provider credentials are required only when the backend sends pushes.
# Never commit the private key or a .p8 file.
# APNS_KEY_ID=
# APNS_TEAM_ID=
# APNS_BUNDLE_ID=com.saied.sa7tot
# APNS_PRIVATE_KEY_PATH=
# APNS_ENVIRONMENT=development
